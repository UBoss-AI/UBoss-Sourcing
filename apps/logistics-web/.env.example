# The API base URL. No trailing slash.
#
# The portal talks to the same API as the console and the storefront, on its
# own route tree under /logistics. There is no second backend.
VITE_API_BASE_URL=http://localhost:4000/api/v1
